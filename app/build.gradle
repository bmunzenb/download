plugins {
    id 'application'
}

application {
    mainClass = 'com.munzenberger.download.app.MainKt'
    applicationName 'download'
}

dependencies {
    implementation project(':core')
    implementation("com.github.ajalt.clikt:clikt:$clikt")
}
